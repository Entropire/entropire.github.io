{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:test": "npm run build && gh-pages -d dist -b live -f --dest test"
  },
  "dependencies": {
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
